export interface CheckoutMenuProps {
  priceTotal: number;
  ticketPrice: number;
  taxes: number;
  tickets: string;
  departureInfo: string;
  arrivalInfo: string;
  destinationDirection: string;
  date: string;
}
