```
src/ 
├── app/ # Инициализация приложения, маршруты, провайдеры <br>
│ ├── App.tsx <br>
│ ├── index.tsx <br>
│
├── pages/ # Страницы (entry point фичей)
│
├── widgets/ # Крупные UI-композиции (из фич и сущностей)
│
├── features/ # Отдельные фичи (логика + UI)
│
├── entities/ # Сущности бизнес-логики (Ticket, User и т.д.)
│
├── shared/ # Общие компоненты, утилиты, конфиги
│ ├── ui/ # Кнопки, инпуты, лоадеры, иконки и т.п.
│ ├── lib/ # debounce, formatDuration, API helpers
│ ├── config/ # Константы, env, пути
│ ├── model/ # Общие типы и интерфейсы
│ └── styles/ # SCSS, темы
│
├── processes/ # Сквозные процессы (например, логика авторизации)
│
└── index.html # Точка входа
```
